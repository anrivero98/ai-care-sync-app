import React from 'react';

const MainHeader = () => {
  const boxStyle = {
    width: '100%',       // Full width of the viewport
    height: '120px',     // Height of the box, adjust as needed
    border: '2px solid black', // Black border
  };

  return (
    <div style={boxStyle}></div>
  );
};

export default MainHeader;
